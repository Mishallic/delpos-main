{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "npm install && cd delpos-client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC"
}
